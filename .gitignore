# Terraform-related files
.terraform/           # Local Terraform state directory
*.tfstate            # Terraform state files
*.tfstate.*          # Backup state files
crash.log            # Crash log for debugging
.terraform.lock.hcl  # Ignore Terraform lock file if it's autogenerated and you don’t want to track it (optional)

# Ignore sensitive files
*.tfvars             # Terraform variable definitions (may contain secrets)
*.tfvars.json        # JSON variable files (may contain secrets)
override.tf          # Override files (local, not shared)
override.tf.json     # JSON override files
*_override.tf        # Any override files
*_override.tf.json   # JSON override files

# General files
*.log                # Log files
.DS_Store            # macOS-specific files
Thumbs.db            # Windows-specific files

# Exclude files related to editor or IDE
.idea/               # JetBrains IDEs (IntelliJ, PyCharm, etc.)
.vscode/             # Visual Studio Code
*.swp                # Vim swap files
*.swo                # Vim swap files

# Ignore generated binary provider files
terraform-provider-*

# Optional: Uncomment this line to track the lock file if needed
# !terraform.lock.hcl
